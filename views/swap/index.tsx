import { FC } from 'react';

import Layout from '@/components/layout';

import SwapContent from './swap-content';

const Swap: FC = () => (
  <Layout>
    <SwapContent />
  </Layout>
);

export default Swap;
